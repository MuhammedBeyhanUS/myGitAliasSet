# myGitAliasSet
Git Alias Set

vi ~/.gitconfig


[alias]

        s = status
        
        a = add
        
        ad = a .
        
        ci = commit -m
        
        co = checkout
        
        cob = co -b
        
        po = pull origin
        
        pod = po development
        
        pom  = po master
